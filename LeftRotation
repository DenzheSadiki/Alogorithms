 public static List<int> rotateLeft(int d, List<int> arr)
  {
        
        for(int i=0; i<d; i++)
        {
            var first =arr[0];
       
            arr =arr.Skip(1).ToList();
            arr.Add(first);
        }
        
        return arr;

    }

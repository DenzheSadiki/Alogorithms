  public static List<int> rotateLeft(int d, List<int> arr)
    {
        
        for(int i=0; i<d; i++)
        {
            var first =arr[0];
            var temp = arr[..^1];
            arr =temp;
            arr.Add(first);
        }
        
        return arr;

    }

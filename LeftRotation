 public static List<int> rotateLeft(int d, List<int> arr)
  {
        
        while(d != 0)
        {
            var first = arr[0];
            arr.Add(first);
            arr.Remove(first);
            d--;
        }

        return arr;

    }
